<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tang.demo.admin.mapper.SysOauthClientMapper">

        <select id="getById" parameterType="string" resultType="com.tang.demo.admin.domain.SysOauthClient" >
            select * from sys_oauth_client  where client_id = #{clientId}
        </select>

<!--        <select id="getClientId">-->
<!--            select client_id from sys_user u,sys_user_client uc where u.user_id=uc.user_id and username=#{username}-->
<!--        </select>-->
</mapper>